sudo su
ls -lart
tar -cvf archive.tar syslog auth.log lastlog
tar -tvf archive.tar
ls -alrt archive.tar
ls -alrt
grep 'error' syslog
grep -i 'error' syslog
find
find . -type f
find . -type d
find . -type f -name "syslog"
find . -type f -name "syslog"
find / -type f -iname "syslog" for particular file
ls -alrt |sort -k 8nr reverse sorting

https://crontab-generator.org/

if [ $1 -gt 10 ];
then
echo "$1 number is positive"
else
echo "$1 number is negative"
fi

echo "Enter Operation to be Done !!"

echo "free for Free Memory"

echo "df for Disk Filesystem"

read input

if [ $input = "free" ]; then
        echo "Performing Free Memory"
        free -m -t
else
        echo "Performing Disk FIle System"
        df -h
fi

for i in "/tmp" "/opt" "/home"
do
        echo "Welcome Directory : $(ls -lart $i)"
done


for i in 1 2 3 4 5
do
echo "Welcome $i times"
done

num=$1
i=1
while [ $i -le $num ]
do
        echo "Value of i: $i"
        i=`expr $i + 1`
done


echo "Enter Operation to be Done !!"

echo "free for Free Memory"
echo "df for Disk Filesystem"
echo "1 for Uptime"
read input

case $input in
 "free") 
        echo "Executing Free Command"
        free -m -t ;;
 1) 
        echo "Executing Uptime Command"
        uptime ;;
 "df") 
        echo "Executing Disk FIle System Command"
        df -h ;;
 *) 
        echo "Provided Wrong inputs !! Trigger Script Again";;
esac

#!/bin/bash
echo -e "Enter Number : \c"
read n
for((i=2; i<=$n/2; i++))
do
  ans=$(( n%i ))
  if [ $ans -eq 0 ]
  then
    echo "$n is not a prime number."
    exit 0
  fi
done
echo "$n is a prime number."





